import SwiftUI

struct SettingsView: View {
    var body: some View {
        Text("Settings")
            .font(.title2)
            .foregroundStyle(.secondary)
            .frame(width: 400, height: 300)
    }
}
